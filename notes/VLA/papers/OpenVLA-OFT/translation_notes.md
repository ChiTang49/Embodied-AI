# translation_notes.md — 2502.19645v2 阅读包说明

## 交付内容

- `paper.md`：全文级中英对照阅读文件（正文段落逐块对齐，图表就近放置）。
- `source_map.json`：稳定锚点表（正文/图注编号、页码、原文、译文、图表位置）。
- `assets/`：从 PDF 原文裁切的 13 张插图与 16 张表格（200 dpi，边界经留白校验）。
- `2502.19645v2.pdf`：原始 PDF 副本，与 `paper.md` 同目录。

## 抽取与整理方法

1. 用 pdfplumber 按“行分组 → 分栏切分 → 段落合并”重建阅读顺序，处理了双栏混排、跨栏标题与跨页段落。
2. 图表不重绘：插图按 PDF 内嵌图像的包围盒裁切；表格按横线范围裁切；每张裁切图的四条边界都做了留白校验（边界行/列必须近乎全白），以确保不切断内容、不混入正文。
3. 表格文字因此不再出现在正文文本块中（它们已包含在表格图片里）。字符级覆盖检查显示：除表格区域外无遗漏字符。
4. 中文译文为逐块人工翻译，保留公式、符号、数字、引用标记与专有名词原形。

## 已知不确定性 / 取舍

- **数学排版**：PDF 中的公式（如 FiLM 表达式、`K = 8`、`[−1, +1]`）以线性文本形式给出，未重建 LaTeX 排版。
- **参考文献**：以原文保留（59 条，未翻译）。PDF 文本层中本来存在的编码噪声（个别作者名的变音符号、标题中词首字母大小写、连字符断行）已按原样保留并只修正了跨行断词，正式引用请以原文 PDF 为准。
- **图注强调样式**：原文图注中的斜体（如 “Left:”“Right:”）在 Markdown 中不再区分样式。
- **表格内容**：以图片形式呈现，未转录为可编辑表格；如需检索表格数值，请查看 `assets/tab*.png`。
- **页码**：所有 `p.N` 均指本地 PDF 的物理页码（共 24 页），与 arXiv v2 版本一致。
