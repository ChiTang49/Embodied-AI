# translation_notes.md — 2511.14759v2 阅读包说明

## 交付内容

- `paper.md`：全文级中英对照阅读文件（正文逐块给出 `**Original:**` 与 `**中文:**`，图/公式/算法就近放置）。
- `source_map.json`：稳定锚点表（正文块、图注、公式、参考文献的原文、译文与页码）。
- `assets/`：从 PDF 原文裁切的 13 张插图、1 个算法框与 14 个公式/公式组（200 dpi）。
- `2511.14759v2.pdf`：原始 PDF 副本，与 `paper.md` 同目录。

## 抽取与整理方法

1. 正文用 pdfplumber 逐词提取（`x_tolerance=1.5`，该 PDF 的词间空格缺失、小型大写字母需按词合并），
   先按行聚类、再按栏边界切分，最后依据缩进与行距合并成段落，恢复双栏阅读顺序。
2. 跨栏/跨页续段、跨行连字符（如 `founda-tion` → `foundation`）与真实复合词（`pre-training`、`fine-grained`）
   由人工维护的规则表逐条判定，`translation_notes` 之外的判断均已核对原文。
3. 模型名中的上下标（π*_{0.6}）在原 PDF 中是独立的文本对象，位置与基线不重合；
   阅读包统一转写为 `π*0.6`（基线模型写作 `π0.6`、`π0.5`），避免抽取噪声。
4. 图、算法框与公式均**按原文裁切**为图片，而不是重绘：公式数量多、含分式与多行推导，
   转写风险高于裁切，故以裁切图为准，另给线性转写与中文说明。
5. 正文引用编号（[1]–[86]）与参考文献表编号一致，未做重编号。

## 已知不确定性 / 取舍

- **公式排版**：公式以裁切图呈现；旁边的“线性转写”是把 PDF 文本层压平后的近似写法，
  上下标、分式、求和范围可能与原排版不完全一致，**引用请以 PDF 原文为准**。
- **参考文献**：86 条参考文献保持英文原文、未翻译（便于检索与引用），条目来自 PDF 参考文献区，
  已按编号重排并去除跨栏错位；个别作者名中的变音符号（如 `Ste´phane`、`Rau´l`、`na¨ıve`）按 PDF 文本层原样保留。
- **图注中的数学**：图注中的下标（如 π*0.6）已按上述规则统一；
  图注里出现的公式符号保留了线性写法。
- **图片裁切范围**：多面板图（图 4、图 7-图 13）按整图包围盒裁切，可能包含图内嵌的标注文字；
  图注文字单独给出中英对照，未并入图片。
- **脚注**：第 III 节与第 IV-B 节的脚注以引用块形式保留在对应位置（含中文翻译）。
- **页码**：所有 `p.N` 均指本地 PDF 的物理页码（共 18 页），与 arXiv v2 版本一致。
- **未被逐字翻译的内容**：作者署名列表、致谢中的机构性表述、参考文献条目、附录 A 的人名清单
  按原文保留（人名不译）；其余正文段落均有逐块中文翻译。

## 术语表（高频技术词）

| 英文 | 中文 | 说明 |
| --- | --- | --- |
| vision-language-action (VLA) model | 视觉-语言-动作模型 | 本文的研究对象 |
| advantage conditioning | 优势条件化 | 以优势的（二值化）指标作为策略输入 |
| RECAP | RECAP | RL with Experience and Corrections via Advantage-conditioned Policies，不译 |
| policy extraction | 策略提取 | 由价值函数导出改进策略的过程 |
| value function | 价值函数 | 预测“到成功的剩余步数”的负值 |
| on-policy / off-policy | 在策略 / 离策略 | 数据与当前策略的关系 |
| flow matching | 流匹配 | 连续动作的生成方式 |
| action chunk / action expert | 动作块 / 动作专家 | π0.6 的动作表示与专用权重 |
| Knowledge Insulation (KI) | 知识隔离 | π0.6 的训练配方 |
| classifier-free guidance (CFG) | 无分类器引导 | 推理时用 β > 1 锐化策略 |
| throughput / success rate | 吞吐量 / 成功率 | 两个主要评估指标 |
| human-gated DAgger | 人类把关的 DAgger | 干预数据采集方式 |
| trust region | 信赖域 | PPO 基线的约束 |
| behavior policy | 行为策略 | 采集数据集的策略 |
